module calculator
ℯ^(1/(2pi))

g = 9.8
v₀ = 200
θ = 45
k= 1/2
a = v₀ * cosd(θ)
projectile(x) = (g/(k*a) + tand(θ))*x + g/k^2*log(1-k/a*x)

projectile(100)

(sin ∘ +)(3,5)

map(first ∘ reverse ∘ uppercase, split("you can compose function like this") )

end